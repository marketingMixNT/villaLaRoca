models:
  VoucherPage:
    meta_title: json
    meta_desc: json
    header_heading: json
    banner: text
    heading: json nullable
    text: json nullable
    relationships:
      hasMany: VourcherPageBlock
  VoucherPageBlock:
    image: text
    text: json
    sort: integer nullable
    relationships:
      belongsTo: VoucherPage

 