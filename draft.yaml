models:
  GalleryPage:
    meta_title: json
    meta_desc: json
    header_heading: json
    banner: text
    heading: json nullable
    text: json nullable

  Gallery:
    category: json
    images: text
    sort: integer nullable